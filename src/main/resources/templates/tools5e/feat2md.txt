---
obsidianUIMode: preview
cssclasses:
- json5e-feat
{#if resource.tags }
tags:
{#for tag in resource.tags}
- {tag}
{/for}{/if}
aliases:
{#each resource.aliases}
- {it}
{/each}
{#if resource.books }
sources:
{#for book in resource.books}
- {book}
{/for}{/if}
SourceType: "Feat"
---
# {resource.name}
{#if resource.category || resource.prerequisite}
*{#if resource.category}{resource.category} {/if}{#if resource.prerequisite}(Prerequisite: {resource.prerequisite}){/if}*
{/if}{#if resource.hasImages }
{resource.showPortraitImage}
{/if}

{resource.text}

{#if resource.hasMoreImages }
{resource.showMoreImages}

{/if}
*Source: {resource.source}*
