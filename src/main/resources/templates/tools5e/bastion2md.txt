---
obsidianUIMode: preview
cssclasses:
- json5e-bastion
{#if resource.tags }
tags:
{#for tag in resource.tags}
- {tag}
{/for}{/if}
aliases:
{#each resource.aliases}
- {it}
{/each}
{#if resource.books }
sources:
{#for book in resource.books}
- {book}
{/for}{/if}
SourceType: "Bastion"
---
# {resource.name}
*{#if resource.prerequisite}Level {resource.level} {/if}Bastion facility*{#if resource.hasImages }
{resource.showPortraitImage}{/if}

{#if resource.prerequisite}
- **Prerequisite:** {resource.prerequisite}
{/if}{#if resource.space }
- **Space:** {resource.spaceDescription}
{/if}{#if resource.hirelings }
- **Hirelings:** {resource.hirelingDescription}
{/if}{#if resource.orders }
- **Order:** {resource.orders.join(", ")}
{/if}{#if resource.text }

{resource.text}
{/if}
{#if resource.hasMoreImages }

{resource.showMoreImages}
{/if}

*Source: {resource.source}*
