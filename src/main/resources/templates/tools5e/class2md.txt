---
obsidianUIMode: preview
cssclasses:
- json5e-class
{#if resource.tags }
tags:
{#for tag in resource.tags}
- {tag}
{/for}{/if}
aliases:
{#each resource.aliases}
- {it}
{/each}
{#if resource.books }
sources:
{#for book in resource.books}
- {book}
{/for}{/if}
SourceType: "Class"
---
# {resource.name}{#if resource.hasImages }
{resource.showPortraitImage}{/if}

## Hit Points

{#if resource.hitDice }
- **Hit Dice:** 1d{resource.hitDice} per {resource.name} Level
- **Hit Points at First Level:** {resource.hitDice} + Constitution Modifier
- **Hit Points at Higher Levels:** add {resource.hitRollAverage} OR 1d{resource.hitDice} + Constitution Modifier  (minimum of 1)
{#else}
- **Hit Dice:** *x* = hit dice specified in the sidekick's statblock (human, gnome, kobold, etc.)
- **Hit Points at First Level:** *x* + Constitution Modifier
- **Hit Points at Higher Levels:** add 1d*x* + Constitution Modifier (minimum of 1)
{/if}

## Becoming {#if resource.name == 'Artificer'}an{#else}a{/if} {resource.name}...

### As a Level 1 Character

{resource.startingEquipment}

{#if resource.multiclassing }
### As a Multiclass Character

{resource.multiclassing}

{/if}
{#if resource.classProgression }
## {resource.name} Class Features

{resource.classProgression}

{/if}
{#if resource.hasMoreImages }
{resource.showMoreImages}

{/if}
{resource.text}

*Source: {resource.source}*
